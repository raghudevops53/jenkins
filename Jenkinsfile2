@Library('roboshop') _

sample.info 'Starting'
sample.warning 'Nothing to do!'

pipeline {
  agent any

  stages {

    stage('Shared Library Demo') {
      steps {
        script {
          print 'hello'
        }
      }
    }
  }
}