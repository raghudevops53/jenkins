@Library('roboshop') _

pipeline {
  agent any

  stages {

    stage('Shared Library Demo') {
      steps {
        script {
          log.info 'Starting'
          log.warning 'Nothing to do!'
        }
      }
    }
  }
}