@Library('roboshop') _

log.info 'Starting'
log.warning 'Nothing to do!'

pipeline {
  agent any

  stages {

    stage('Shared Library Demo') {
      steps {
        script {
          print 'hello'
        }
      }
    }
  }
}